import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    completedTasks: {
        marginTop: '20px',
        width: '285px',
    },
    completedTask: {
        marginTop: '5px',
        padding: '10px',
    },
    completedButton: {
        width: '100%',
        margin: '10px 0px',
    }
});
